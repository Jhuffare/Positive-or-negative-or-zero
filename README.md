# Positive-or-negative-or-zero
#include<iostream.h>
int main()
{
  int n;
  cin>>n;
  if(n>0)
  {
    cout<<"positive number";
    }
  else if(n==0)
  {
    cout<<"number is zero";
    }
  else
  {
    cout<<"negative number"
    }
}
